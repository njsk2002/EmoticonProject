<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.mapper.FileMapper">
	<select id="getImageList" resultType="kr.co.domain.UploadDTO">
		select * from imagefile where i_no=#{i_pno}
	</select>
	<select id="getImageList1" resultType="kr.co.domain.UploadDTO">
		select * from imagefile;
	</select>
</mapper>